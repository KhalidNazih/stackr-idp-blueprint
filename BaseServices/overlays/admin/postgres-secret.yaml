apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
# Data will be automatically populated by the Secrets Store CSI Driver
# via the postgres-vault-secrets SecretProviderClass.
# This prevents hardcoded passwords in the repository.
