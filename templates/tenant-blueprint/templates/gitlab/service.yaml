{{- if .Values.services.gitlab.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.tenant_id }}-gitlab
spec:
  type: ClusterIP
  ports: [{ port: 80, targetPort: 80}]
  selector: { app: gitlab, tenant: {{ .Values.tenant_id }} }
{{- end }}
