{{- if .Values.services.gitlab.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.tenant_id }}-gitlab-pvc
spec:
  storageClassName: managed-csi 
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 20Gi
{{- end }}
