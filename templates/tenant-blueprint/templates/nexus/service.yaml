{{- if .Values.services.nexus.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.tenant_id }}-nexus
spec:
  type: ClusterIP
  ports: [{ port: 80, targetPort: 4180}] 
  selector: { app: nexus, tenant: {{ .Values.tenant_id }} }
{{- end }}
