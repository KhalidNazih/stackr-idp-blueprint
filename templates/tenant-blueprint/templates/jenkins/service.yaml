{{- if .Values.services.jenkins.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.tenant_id }}-jenkins
spec:
  type: ClusterIP
  ports: [{ port: 8080, targetPort: 8080}]
  selector: { app: jenkins, tenant: {{ .Values.tenant_id }} }
{{- end }}
